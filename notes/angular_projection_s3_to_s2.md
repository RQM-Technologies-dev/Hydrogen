# S^3 to S^2 angular projection note (legacy dimension-counting bridge)

This note is retained as the older dimension-counting bridge.
For the stronger operator-level statement, see `notes/pi_k_angular_intertwiner.md`.

## Dimension identity (retained)
At fixed shell index \(K\):
\[
\dim \mathcal H_K(S^3)=(K+1)^2
\]
and
\[
\sum_{\ell=0}^{K}(2\ell+1)=(K+1)^2.
\]
So shell-wise \(S^3\) counting matches \(\ell\)-sector counting on \(S^2\):
\[
\mathcal H_K(S^3)\rightsquigarrow\bigoplus_{\ell=0}^{K}\mathcal H_\ell(S^2).
\]

## Reader guidance
Use this file for quick counting context only.
Use `notes/pi_k_angular_intertwiner.md` for the primary v1 angular bridge statement \(\Pi_K^{\mathrm{ang}}\).


## Honesty boundary
This legacy note provides dimension-counting context only and does not claim a completed physical intertwiner construction.
